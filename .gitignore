# Add to gitignore all garbage files that should never be part of the final system.
# this excludes:
# - chores.md since its the current planning resource, although it will also be deleted on the final deployment.
# - compose.yaml as it represents a crucial "off-the-books" part of infrastructure for k8s ( The ESO)

.env
vmisc/**
taloslop/**
envoy-crds.yaml
test-image.yaml
sys-bootstrap.sh