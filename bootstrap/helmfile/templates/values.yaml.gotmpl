{{- $main := fromYaml (readFile (printf "../../kubernetes/deployment/platform/%s/%s/app/values.yaml" .Release.Namespace .Release.Name)) -}}
chart: {{ $main.spec.url }}
version: {{ $main.spec.ref.tag }}