{{- if and (eq .Values.kind "Deployment") .Values.volumeMounts }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: block-pvc
spec:
  accessModes: [ "ReadWriteOnce" ]
  storageClassName: "block"
  resources:
    requests:
      storage: 10Gi
{{- end }}
