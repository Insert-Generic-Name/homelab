apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
spec:
  type: {{ .Values.appServiceType | default "ClusterIP" }}
  selector:
    app: {{ .Values.appName }}
  ports:
  - port: {{ .Values.appPort }}
    targetPort: {{ .Values.appPort }}
    protocol: {{ .Values.appPortProtocol }}