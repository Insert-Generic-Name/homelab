apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager
spec:
  chartRef:
    kind: OCIRepository
    name: cert-manager
  interval: 5m
  values:
    replicaCount: 1 #changemelater
    webhook:
      replicaCount: 1 #changemelater
    prometheus:
      enabled: true
      servicemonitor:
        enabled: false # chagemelater
    podDisruptionBudget:
      enabled: true
    crds:
      enabled: true
