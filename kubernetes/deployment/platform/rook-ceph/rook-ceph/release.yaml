apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rook-ceph
spec:
  chartRef:
    kind: HelmChart
    name: rook-ceph
  values:
    csi:
      serviceMonitor:
        enabled: true
    monitoring:
      enabled: false #changeme
